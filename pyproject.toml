[project]
name = "robotsix-cai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "PyYAML>=6",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501", "F403", "F405"]
