# Module index — owned and maintained by cai-review-docs.
# On every PR that adds, renames, or deletes a tracked file, cai-review-docs
# updates this file and the matching docs/modules/<name>.md narrative.
#
# Schema:
#   modules:
#     - name:        short identifier (matches narrative filename stem)
#       description: one-line summary of the module's purpose
#       globs:       list of fnmatch-style patterns, repo-root-relative,
#                    that enumerate the files belonging to this module
#       narrative:   path to the per-module narrative markdown file
modules:
  - name: dispatcher-cli
    description: Top-level CLI entry point and root-level shims.
    globs:
      - "cai.py"
      - "parse.py"
      - "publish.py"
    narrative: "docs/modules/dispatcher-cli.md"

  - name: cai-lib
    description: Core library — FSM, dispatcher, state handlers, helpers.
    globs:
      - "cai_lib/**"
    narrative: "docs/modules/cai-lib.md"

  - name: agents
    description: Declarative Claude subagent definitions.
    globs:
      - ".claude/agents/**"
      - ".claude/settings.json"
    narrative: "docs/modules/agents.md"

  - name: workflows
    description: GitHub Actions CI/CD workflows.
    globs:
      - ".github/workflows/**"
    narrative: "docs/modules/workflows.md"

  - name: docs-site
    description: User-facing documentation, README, and agent guidance files.
    globs:
      - "docs/**"
      - "README.md"
      - "CODEBASE_INDEX.md"
      - "CLAUDE.md"
      - "LICENSE"
    narrative: "docs/modules/docs-site.md"

  - name: container
    description: Container build, orchestration, and installer artefacts.
    globs:
      - "Dockerfile"
      - "docker-compose.yml"
      - "entrypoint.sh"
      - "install.sh"
      - ".env.example"
      - "workspaces.json.example"
      - ".gitignore"
      - "pyproject.toml"
    narrative: "docs/modules/container.md"

  - name: scripts
    description: Maintenance and code-generation scripts.
    globs:
      - "scripts/**"
    narrative: "docs/modules/scripts.md"

  - name: tests
    description: Pytest suite.
    globs:
      - "tests/**"
    narrative: "docs/modules/tests.md"
