# agents-config

Claude Code harness configuration for the agents pipeline. Defines the
permission allowlist, hooks, env vars, and defaults used when the
headless `-p` mode invokes any of the subagents under `.claude/agents/`.

## Entry points
- `.claude/settings.json` — Claude Code harness configuration.
