---
title: Workflows
nav_order: 5
has_children: true
---

# Workflows

Each cai-* CLI is implemented as a `pydantic_graph.Graph`. The diagrams on the child pages are regenerated from the live graph definitions by `scripts/gen_workflow_graphs.py` (wired as a pre-commit hook), so they always match the code.
